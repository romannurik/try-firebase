{
  "name": "simple-firebase-try",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^9.8.2",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start:web": "PORT=8000 BROWSER=none react-scripts start",
    "start:firebase": "firebase emulators:start --only firestore,ui",
    "start": "npm-run-all --parallel start:web start:firebase",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
